import { createApp } from 'vue' // está importando a função createApp, que é responsável por criar a aplicação Vue
import App from './App.vue' //App.vue é o componente inicial, tudo começa a partir dele

createApp(App).mount('#app') // cria o app e renderiza dentro do elemento <div id="app">


